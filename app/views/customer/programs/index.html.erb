<% @title = "プログラム⼀覧" %>
<h1><%= @title %></h1>

<div class="table-wrapper">
  <%= paginate @programs %>

  <table class="listing">
    <tr>
      <th>タイトル</th>
      <th>申し込み開始⽇時</th>
      <th>申し込み終了⽇時</th>
      <th>最⼩参加者数</th>
      <th>最⼤参加者数</th>
      <th>アクション</th>
    </tr>
    <%= render partial: "program", collection: @programs %>
  </table>

  <%= paginate @programs %>
</div>